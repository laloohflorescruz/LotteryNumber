@model string
@using Lotery.Models


<form asp-action="DeleteApplicationRole" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Borrar Rol" })

    <div class="modal-body form-horizontal">
        realmente desea borrar este rol @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Borrar" })
</form>

