@model string
@using Lotery.Models


<form asp-action="DeleteUser" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Borrar Usuario" })

    <div class="modal-body form-horizontal">
        Desea borrar realmente este usuario @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Borrar" })
</form>

